
definicao_de_cenarios {
    cenario_login {
        nome = "Login de usuário válido";
        dados_entrada = {
            usuario = "usuario@example.com";
            senha = "senha123";
        };
        acoes = {
            acessar_tela_login();
            preencher_campo_usuario(usuario);
            preencher_campo_senha(senha);
            clicar_botao_login();
        };
        resultados_esperados = {
            tela_principal_deve_estar_visivel();
            mensagem_de_bem_vindo_deve_ser_exibida();
        };
    }
}

definicao_de_acoes {
    acessar_tela_login {
        tipo = "navegacao";
        rota = "/login";
    }

    preencher_campo_usuario {
        tipo = "input";
        seletor = "id";
        valor_seletor = "input-usuario";
    }

    preencher_campo_senha(params) {
        tipo = "input";
        seletor = "id";
        valor_seletor = "input-senha";
    }

    clicar_botao_login {
        tipo = "clique";
        seletor = "id";
        valor_seletor = "btn-login";
    }
}

definicao_de_configuracao {
    ambiente = "teste";
    url_base = "https://www.example.com/";
    browser = "chrome";
}

teste_completo {
    nome = "Suite de testes de login";
    configuracao = ambiente_teste;
    cenarios = cenario_login;
}